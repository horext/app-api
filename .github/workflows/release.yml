name: Release Workflow

on:
  workflow_call:
    inputs:
      java-version:
        required: false
        type: string
        default: "25"
    outputs:
      image-tag:
        description: "Docker image tag"
        value: ${{ jobs.release.outputs.tags }}

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/${{ github.ref_name }}

jobs:
  release:
    name: Build and Push OCI Image
    runs-on: ubuntu-latest
    outputs:
      tags: ${{ steps.image.outputs.tag }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Set up GraalVM ${{ inputs.java-version }}
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: graalvm
          java-version: ${{ inputs.java-version }}
          cache: gradle
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: false

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6.3.0
        with:
          cache-disabled: true

      - name: Make Gradlew executable
        run: chmod +x gradlew

      - name: Resolve image tag
        id: image
        run: echo "tag=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> "$GITHUB_OUTPUT"

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull previous image
        run: docker pull ${{ steps.image.outputs.tag }} || true

      - name: Build OCI image with known-good builder
        run: |
          ./gradlew bootBuildImage \
            --imageName=${{ steps.image.outputs.tag }} \
            --builder=paketobuildpacks/builder-noble-java-tiny@sha256:5d484671a3472dc6dd323c297923f33262aa0d4dbcbbcceac3d6c69586d140be \
            --no-daemon

      - name: Push OCI image
        run: docker push ${{ steps.image.outputs.tag }}
